apiVersion: v1
kind: Service
metadata:
  name: nextjs-service
  namespace: smartsociety
spec:
  selector:
    app: nextjs-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP